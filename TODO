https://github.com/gregallensworth/L.Control.jQueryDialog/blob/master/example/index.html

restore, about / kml download links!
{ site.description | escape }}
Curated by [Jesse Spielman](http://heavyimage.com), with plenty of suggestions from friends!


what to do on detail pages:
    cache fq business id on .md file
    turn off rendering of locations
    code:

        // ZOMG: https://gist.github.com/bsudekum/6063237
        $.ajax({
        type: "GET",
        dataType: "jsonp",
        cache: false,
        url: 'https://api.foursquare.com/v2/venues/search?ll=' + 40.78 + ',' + -73.96 + '&limit=30&client_id=3ODULPRCZOZL43JVOBNUBAUPXD5MXKBX24DOUZYNO0V3SEUL&client_secret=MBM0IB0YS1JZB2QLZ3WZDHSZAC10JOO0JY4VGDOF0IU3E5AH&v=20180509',
        success: function(data){
        console.log(data)
        var base = data.response.venues

        $.each(base, function(index){
            var url = base[index].url;
            var name = base[index].name;
            console.log(url)
            console.log(name)
            })
        }
        });

    overlay pops up:
        make overlay
        show:
            name
            my review
            # of locations
            recommender
            display {{content}}
        foursquare!
            FS review
            photos!
            phone number
            website
            description
            (or missing FS business ID)


center to where you are now:
    navigator.geolocation.getCurrentPosition(function(location) {
    console.log(location.coords.latitude);
    console.log(location.coords.longitude);
        console.log(location.coords.accuracy);
        });

content:
    fix script to import spaces as underscores!
    for-real data import from various mymaps repos
        london
        upstate ny
        all of nyc
            brookyln strategist / hanah sushi / best bits of central park!

MD improvements:
    per-location metadata:
        formalize recommender of location (hannah, lisa etc)
        add whether I've visited or not
        date added...?
        categorize restaurants!
        size based on essentialness?

kmz import script:
    support kmls as well
    make layer remapping a bit easier to understand...

input methods tool:
    easy way to add new locations, via binscript:
        * find lat / lon
        * scrape from google?

cbbs:
    RSS feed of recent locations (last_modified_at sucks!)
    ways to add more complex features (walks / hikes?) https://leafletjs.com/examples/geojson/
    add mobile support: Mobile: https://leafletjs.com/examples/mobile/
    kml export:
        include {{ content }} or other MD?
    auto fit bounds to selection of points:
        var group = new L.featureGroup([marker1, marker2, marker3]);
        map.fitBounds(group.getBounds());
        equidistant between NY / Montral but zoom was wrong!
